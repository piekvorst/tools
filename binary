#!/usr/bin/env rc
ifs = '
'

# binary /bin/sh && echo sh is binary
# binary $% || echo $% is not binary

. 9.rc

if (test ! -r $1 -o ! -f $1)
	exit 1

{diff /dev/null $1 || status = ''} |sed 1q |grep -i binary >/dev/null
